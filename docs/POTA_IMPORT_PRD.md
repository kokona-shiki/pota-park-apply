针对 pota 导入功能，我发现现在的做法缺失了很多信息，而且也有很多不对的地方

现在，我对公园的导入需求进行调整：

1. 访问 318 接口，仅为了获取 POTA CN 的 POTA ID 列表，以及 激活次数和 QSO 信息

2. 具体步骤：

一、访问 318 接口，获取列表

二、通过 @protocols/query_park.log 接口，具体的 response 详见 @protocols/query_park_response.json 
获取公园的真实具体信息

三、结合 二 的数据，调整公园信息具体获取的办法：
首先，公园名称直接获取 name 即可，不需要再按照中文、英文拆分，直接读取 name 填入
然后，从中获取
经纬度信息
parkComments 作为我们公园的 description
accessMethods 和 activationsMethods
website
locationDesc，这是公园省份的 ISO-3166，它可能有多个，用英文逗号区分，记得在前端显示为具体的省份简称
公园类型通过 name 和 parktypeDesc 得出，优先通过 name 判断，如果 name 无法判断，则通过 parktypeDesc 判断，如果两者都无法判断，则将公园设为「未处理的公园」

四、query_park 接口必须有延时处理，延时时间不得少于 1 秒，延时时间需要随机变化，延时时间不得大于 3 秒

五、如果某个公园的 query_park 接口失败，重试 2 次，如果仍无法获取，则将此公园设为「未处理的公园」，公园名称填入 318 接口的中文部分名称
当 POTA 地图代表处理这种公园时，重新调用 query_park 接口获取信息之后导入系统，但是公园类型要采用 POTA 地图代表指定的公园类型

六、某个公园的 query_park 接口重试两次仍然失败，被标为「未处理的公园」后，继续处理下一个公园，不要停止整个导入过程

七、因为需求变化后，POTA 导入的时间较长，因此，所有 POTA 导入采用任务队列形式，所有的导入都形成对应的任务，通过任务队列进行调度，不要阻塞前端页面，每次正在执行的 POTA 导入任务只能有一个

八、POTA 导入任务要与对应的操作人关联，如果已经有任务提交到任务队列未执行或者正在执行的，/pota-import 界面点击导入按钮提示「您的 POTA 导入任务正在执行，请稍后再试」，或者「您的 POTA 导入任务正在等待执行，请稍后再试」，不要重复提交任务

九、POTA 地图代表手动发起的 POTA 导入任务完成后，需要在前端给 POTA 地图代表弹对话框提示，对话框要符合 MUI 规范，不能是 alert 框；
如果 POTA 地图代表已经查看过对应任务的提示了，再进入网页不再重新提示

十、POTA 导入任务的队列最大长度为 5，如果超过 5 个，则对手动导入功能提示「POTA 导入任务队列已满，请稍后再试」
对于自动导入任务，则当次不再执行，等待下次自动导入任务执行


----------------------

请与我确认所有需求细节，我明确同意之后，你才能进行编码














###########################################

1. 318 接口指的是 https://api.pota.app/entity/parks/318 接口，在目前的首页中会访问
query_park 接口请查看 @protocols/query_park.log 接口，具体的 response 详见 @protocols/query_park_response.json
没有现成的调用代码，请你自行实现

2. 公园类型判定规则在目前的 POTA 导入过程中已经存在，name 即为中文部分，parktypeDesc 即为英文部分，请你根据现有的规则实现

3. 是的，沿用现有的表
我需要存储 POTA_ID，name，失败原因；
重试次数和原始 response 都是失败原因的一种，直接结合成一个用户友好信息填入失败原因即可

4. 内存队列，服务重启队列清空不需要恢复
需要任务状态

5. 自动导入目前是通过 node-cron 触发的，具体请看相关代码

6. 是的
需要后端记录已读状态

7. 是的，只有 POTA 地图代表可以访问和操作 POTA 导入的相关信息和功能
注意不要在前端露出具体的角色和权限信息，用户角色仅在后端判断，前端依靠后端返回的 boolean 值或者接口是否 HTTP 400 系列错误来判断是否有权限

8. 全局唯一

9. 是的，总共 3 次尝试
记录失败原因

10. 我确认 @shared 文件夹中有 ISO-3166 的 mapping 文件，请你直接使用

--------------------

请与我确认所有细节，我明确同意之后，你才可以编码



############################################  


最终关键点确认

1. 任务状态值在后端存储为英文，在前端要显示中文

2. 接受

3. query_park 要改为「查询 POTA 公园」


请做好权限判断，注意本次的 POTA 导入的所有功能，只能有具备相应权限的用户角色才能读取和操作
前端不要做用户角色判断，仅通过后端返回的 boolean 值或者接口是否 HTTP 400 系列错误来判断是否有权限


___________________________

请与我确认所有细节，我明确统一之后，你才能进行编码