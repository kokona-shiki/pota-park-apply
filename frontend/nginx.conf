# 如果上游网关（TLS 终止）已经设置了 X-Forwarded-Proto，这里要保留它，
# 否则后端无法判断 req.secure，进而无法在 HTTPS 场景下设置 Secure Cookie。
map $http_x_forwarded_proto $proxy_x_forwarded_proto {
  default $http_x_forwarded_proto;
  '' $scheme;
}

server {
  listen 80;

  # 前端静态资源
  root /usr/share/nginx/html;
  index index.html;

  # 后端 API 反向代理（同源访问，避免浏览器 CORS 复杂度）
  location /api/ {
    proxy_pass http://backend:3000;
    proxy_http_version 1.1;
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $proxy_x_forwarded_proto;
  }

  # 第三方 API 反向代理：/pota-api/* -> https://api.pota.app/*
  # 目的：让前端用同源请求访问，避免浏览器 CORS（尤其是 Authorization header 会触发预检）
  location /pota-api/ {
    proxy_pass https://api.pota.app/;
    proxy_http_version 1.1;
    proxy_ssl_server_name on;
    proxy_set_header Host api.pota.app;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $proxy_x_forwarded_proto;
  }

  # SPA 路由回退
  location / {
    try_files $uri $uri/ /index.html;
  }
}
